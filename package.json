{
  "name": "ek-trade-links",
  "version": "1.3.1",
  "description": "Trade links for an EK herald-stacking CI Elementalist — fubgun's budget and mirror PoBs as searches",
  "private": true,
  "scripts": {
    "css": "tailwindcss -i src.css -o styles.css --minify",
    "test": "node scripts/test.js",
    "qa": "node scripts/qa.mjs && node scripts/interact.mjs",
    "check": "npm test && npm run qa"
  },
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "axe-core": "^4.10.0",
    "playwright": "^1.62.1",
    "tailwindcss": "^4.3.3"
  }
}
